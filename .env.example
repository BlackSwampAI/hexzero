# Server-only OpenRouter configuration. Never use NEXT_PUBLIC_ for this key.
OPENROUTER_API_KEY=replace-with-your-openrouter-key

# Server-only Jev key for zero-swarm experiments. Never use NEXT_PUBLIC_.
TYPESAFE_API_KEY=replace-with-your-typesafe-key

# The scripted provider is exclusively for deterministic automation. Normal
# interactive development must leave this unset so every turn uses OpenRouter.
# HEXZERO_PROVIDER=scripted
# Deprecated compatibility alias (HEXZERO_PROVIDER takes precedence):
# AGENTBORNE_PROVIDER=scripted

# Optional local experiment archive path.
# HEXZERO_EXPERIMENT_DB=.hexzero/experiments.sqlite
# Deprecated compatibility alias (HEXZERO_EXPERIMENT_DB takes precedence):
# AGENTBORNE_EXPERIMENT_DB=.agentborne/experiments.sqlite

# Development-map center (Toledo, Ohio) and H3 resolution.
NEXT_PUBLIC_DEV_MAP_LATITUDE=41.6528
NEXT_PUBLIC_DEV_MAP_LONGITUDE=-83.5379
NEXT_PUBLIC_DEV_MAP_H3_RESOLUTION=9
# Optional browser API override. Normal development uses the Next.js proxy.
# NEXT_PUBLIC_GAME_API_BASE_URL=http://127.0.0.1:8787/api/simulation
